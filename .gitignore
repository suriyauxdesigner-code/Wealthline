# dependencies
/node_modules

# next.js build output
/.next/
/out/
next-env.d.ts

# production build
/build

# env files (never commit secrets)
.env
.env.local
.env.*.local

# vercel
.vercel

# typescript
*.tsbuildinfo

# tesseract.js language data — cached to the working directory when its OCR
# pipeline runs under Node (e.g. local testing), not needed in the repo
*.traineddata

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# editor / OS
.DS_Store
.vscode/*
!.vscode/extensions.json
